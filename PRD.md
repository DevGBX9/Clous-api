# Instagram Username Checker API - PRD (Product Requirements Document)

## 📋 معلومات المشروع

| الحقل | القيمة |
|-------|--------|
| **اسم المشروع** | Instagram Username Checker API |
| **المسار** | `c:\Users\Windows user\Desktop\Clous api\` |
| **اللغة** | Python 3 |
| **الإطار** | Flask + httpx (async) |
| **آخر تحديث** | 2026-01-12 |

---

## 🎯 الهدف

API يبحث عن أسماء مستخدمين Instagram متاحة (5 أحرف) بسرعة وكفاءة.

---

## 📁 هيكل الملفات

```
Clous api/
├── app.py          # الكود الرئيسي
└── proxies.txt     # 90 بروكسي (http://user:pass@ip:port)
```

---

## ⚙️ كيف يعمل

### 1. Endpoint الرئيسي
```
GET /search → يرجع يوزرنيم متاح
```

### 2. آلية العمل
```
1. توليد batch من الـ usernames (بعدد البروكسيات)
2. إرسال طلبات متوازية عبر البروكسيات
3. استخدام asyncio.as_completed() للإرجاع الفوري
4. أول available يظهر → إرجاع النتيجة + إلغاء الباقي
```

### 3. Instagram API المستخدم
```
POST https://i.instagram.com/api/v1/users/check_username/
Data: {username, _uuid}
```

### 4. الاستجابات المتوقعة
```json
{"available": true, ...}   // متاح ✅
{"available": false, ...}  // مأخوذ ❌
{"message": "Please wait a few minutes..."} // Rate Limit ⚠️
```

---

## 🛡️ نظام التخفي (Full Device Fingerprint)

### المعرفات المولدة لكل طلب:
- `device_id` (android-xxx)
- `phone_id` (UUID)
- `guid` (UUID)
- `adid` (Google Advertising ID)
- `google_adid`
- `family_device_id`

### الـ Headers (18 header):
```
User-Agent, X-IG-Device-ID, X-IG-Android-ID,
X-IG-Connection-Type, X-IG-Bandwidth-*, X-IG-Capabilities,
X-Bloks-Version-Id, X-Pigeon-Session-Id, X-MID, ...
```

### قاعدة بيانات الأجهزة (7 أجهزة):
- Samsung (SM-G998B, SM-A525F)
- Google (Pixel 7 Pro, Pixel 6a)
- OnePlus (LE2123)
- Xiaomi (M2101K6G)
- OPPO (CPH2207)

### إصدارات Instagram (5 إصدارات):
```
269.0.0.18.75, 268.0.0.23.118, 267.0.0.19.93, 266.0.0.22.105, 265.0.0.19.301
```

---

## 📊 توليد اليوزرنيمات

### الوضع الحالي (مع رموز _ و .):
```python
patterns = [
    "L_LLL", "LL_LL", "LLL_L", "LLLL_",  # underscore
    "L.LLL", "LL.LL", "LLL.L",            # dot
    "L_L.L", "L.L_L", "L_LL_",            # mixed
]
```

### المشكلة:
- اليوزرات مع رموز = أقل توفراً = فحوصات كثيرة (1000+) = rate limits

### الحل الموصى به:
```python
# بدون رموز - أسرع بكثير
return letter + 4 أحرف عشوائية  # مثال: uk5gw
```

---

## 📈 نتائج الاختبارات

### بدون رموز (uk5gw):
| المقياس | القيمة |
|---------|--------|
| المدة | ~1.7 ثانية |
| الفحوصات | 1-5 |
| Rate Limits | 0-2/90 |

### مع رموز (g0v_p):
| المقياس | القيمة |
|---------|--------|
| المدة | 10-30 ثانية |
| الفحوصات | 1000+ |
| Rate Limits | 90/90 ⚠️ |

---

## 🔧 الإعدادات (CONFIG)

```python
CONFIG = {
    "API_URL": 'https://i.instagram.com/api/v1/users/check_username/',
    "TIMEOUT": 30,           # ثانية
    "REQUEST_TIMEOUT": 5.0,  # ثانية لكل طلب
    "PROXIES_FILE": "proxies.txt",
}
```

---

## 📝 التحسينات المطبقة

1. ✅ نقل البروكسيات لملف خارجي `proxies.txt`
2. ✅ تغيير Endpoint من `/accounts/create/` إلى `/users/check_username/`
3. ✅ استخدام `asyncio.as_completed()` للإرجاع الفوري
4. ✅ إلغاء المهام عند إيجاد يوزر متاح
5. ✅ نظام بصمة كاملة للتخفي
6. ✅ تتبع البروكسيات المحظورة بشكل فردي

---

## ⚠️ المشاكل المعروفة

1. **Rate Limits مع اليوزرات الخاصة**
   - السبب: يوزرات مع `_` و `.` أقل توفراً
   - الحل: استخدام يوزرات بدون رموز

2. **90/90 بروكسي محظورة**
   - السبب: كثرة الفحوصات (1000+)
   - الحل: تقليل عدد الفحوصات

---

## 🚀 للتشغيل

```bash
cd "c:\Users\Windows user\Desktop\Clous api"
python app.py
```

ثم:
```
GET http://localhost:5000/search
```

---

## 📋 المهام المستقبلية المحتملة

- [ ] العودة لليوزرات البسيطة (بدون رموز)
- [ ] إضافة delay بين الـ batches
- [ ] تدوير البروكسيات المحظورة
- [ ] إضافة endpoint لفحص يوزر معين
- [ ] حفظ اليوزرات المتاحة في ملف

---

## 👤 المؤلف

@GBX_9 (Original Helper)
